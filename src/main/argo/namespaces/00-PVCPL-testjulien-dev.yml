kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: testjulien-dev-pipeline-claim
  namespace: testjulien-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ocs-storagecluster-ceph-rbd
  volumeMode: Filesystem
status:

